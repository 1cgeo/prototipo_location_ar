// Path: routes\ARViewRoute.tsx
import CameraView from '@/features/ar/components/CameraView';

/**
 * Rota para a visualização de Realidade Aumentada
 */
const ARViewRoute = () => {
  return <CameraView />;
};

export default ARViewRoute;
