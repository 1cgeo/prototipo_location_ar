// Path: types\ar.js\index.d.ts
declare module 'ar.js/aframe/build/aframe-ar' {
  // This is just a side-effect import, so we don't need to export anything
}

declare module 'aframe' {
  // This is just a side-effect import, so we don't need to export anything
}
